# 整合核验

## 二次审查 2026-09-21
- 原生thread_items唯一位置索引按(thread_id,rollout_ordinal)，旧inspect仅查turn内冲突会导致整批事务失败；已与原生约束对齐。
- 原先item_completed既用于原生ID匹配又加入普通事件集合，重复正文可消费同一证据两次。现在两类证据分开消费，普通事件数量不匹配明确核查。
- 目标按正文去重不能区分同文新目标；现在按目标ID和createdAt（无ID时createdAt）+原文识别，并生成稳定恢复ID。
- 已有空轮次也必须核对首条附件/响应/原生投影身份，后续文字不能借用悬空首条ID。截断JSON尾行不再成功缓存。
- 安装前追加审核发现并修复：单响应区间多个完成事件的身份歧义、旧版目标恢复跨分片重复；均有最小回归测试，审核代理复核通过。

- 起始分支 codex/local-combined-2250，HEAD b2993b9。
- 初始未提交 App.tsx 和 i18n-en.ts：本地脚本更新计数和更新按钮，全部保留。
- 备份 C:/Users/lucy/Desktop/codex-combined-backup-20260920-035620。
- 5 个现有文件的会话修复补丁 git apply --check 通过后应用，无文本冲突；3 个新文件仅在目标不存在时添加。
- 未复制原分支整份 App.tsx/lib.rs，不携入无关 mobile-relay 和 AGENTS 改动。

## 延期核查 2026-09-20
- 原有检查在核验 userMessage 是否存在前就判断轮次状态，导致已存在消息只因旧 inProgress 被延期。
- 很多缺失轮次涉及分片 rollout；需要通过原生事件身份核验，不能只根据正文或更新时间推测。
- 问答卡片的灰色问题来自模型，下方 answer 是用户选择；原生将整体存为 userMessage，不能删除或当作纯模型输出。
- 目标分支已新增其他会话提交 ed18bdf 等，当前工作区干净，继续在其之上改动，保留这些修复。
- 延期 174 项中 25 个旧 inProgress 的消息已存在，123 个为分片归属遗漏造成假缺失；优先完整原生 ID/位置/全文锚点，不仅按目录表。
- 副本发现 state 路径可指向分片但仍保留父 ID；严格锚点与目录身份须同时仲裁，全库已有相同 turn_id 时禁止补建，避免跨任务复制。
- 最终真实修复 28 条消息、9 个历史轮次；已有用户消息修改/删除数均 0，新增跨任务重复轮次数 0，旧轮次状态改动 0。
- Windows 任务栏目标已置于问答卡片前；卡片全文保持不变，模型问题与用户回复不混淆。
- 最终仅 1 个等待：本会话当前活动目标；仅 1 个需核查：无效 JSON 日志。尚未声称当前活动目标已完成或实际计数已经 0。
- 后续核验：上述活动轮次正常结束后已自动满足恢复条件。真实等待已为0，重复检查仍0；剩余1项日志损坏仍明确需核查。
