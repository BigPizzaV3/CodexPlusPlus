# 本地整合与发布约定

本地统一分支：`codex/local-combined-2250`。
唯一构建和安装来源：`C:\Users\lucy\Desktop\CodexPlusPlus-weixin-cli-fix`。

继续本会话时，应先检查该目录未提交改动，以差异合并会话修复，保留其他功能。不得以其他分支的完整文件覆盖，不得从其他目录构建的 EXE 更新安装目录。

安装前备份现有程序，先完成前端构建再构建 Rust，更新后核对安装文件与构建产物 SHA256 一致。保留正在执行的 Codex 会话，旧启动器待正常重启后切换。

对外 PR 按功能保持独立。任何新建或追加到现有 PR 的操作须先询问用户。
