---
name: xuan-usage
description: Query relay usage without exposing credentials or replacing official token history.
---

Use `usage_query` for relay-provider usage. Use the official Codex session
history or host API for per-thread token counts instead of copying that history
into the plugin database. Never request or reveal API keys.
